---
title: 'Get Voices'
openapi: 'GET /voices'
---

Retrieve a list of all available voices for text-to-speech synthesis.

## Endpoint

```
GET /voices
```

## Authentication

Requires API key authentication via `x-api-key` header.

## Response

Returns an array of voice objects.

### Response Schema

Each voice object contains:

| Field | Type | Description |
|-------|------|-------------|
| `voice_id` | string | Unique identifier for the voice (used in text-to-speech requests) |
| `name` | string | Human-readable name of the voice |
| `description` | string \| null | Detailed description of the voice characteristics |
| `gender` | string \| null | Gender of the voice (`male`, `female`, or `null`) |
| `age` | string \| null | Age category of the voice (`middle`, `elderly`, or `null`) |
| `languages` | array[string] | List of language codes supported by the voice (e.g., `["ar", "en"]`) |
| `dialect` | array[string] | List of dialects supported by the voice (e.g., `["fusha", "emirati", "najdi"]`) |
| `type` | string \| null | Voice type (`neural` or `null`) |
| `sample_url` | string | URL to an audio sample of the voice |

### Example Request

```bash
curl -X GET "https://api.faseeh.ai/api/v1/voices" \
  -H "x-api-key: YOUR_API_KEY"
```

### Example Response

```json
[
  {
    "voice_id": "ar-msa-male-2",
    "name": "hassan",
    "description": "Modern Standard Arabic male voice with clear pronunciation",
    "gender": "male",
    "age": "middle",
    "languages": [
      "ar",
      "en"
    ],
    "dialect": [
      "fusha"
    ],
    "type": "neural",
    "sample_url": "https://pub-a2eeb500293c41df9e06b82b4178468a.r2.dev/samples/ar-msa-male-2.wav"
  },
  {
    "voice_id": "ar-msa-male-1",
    "name": "khalid",
    "description": "Modern Standard Arabic male voice",
    "gender": "male",
    "age": "middle",
    "languages": [
      "ar",
      "en"
    ],
    "dialect": [
      "fusha"
    ],
    "type": "neural",
    "sample_url": "https://pub-a2eeb500293c41df9e06b82b4178468a.r2.dev/samples/ar-khalid-male.wav"
  },
  {
    "voice_id": "ar-uae-male-2",
    "name": "saif",
    "description": "Emirati male voice with warm tone",
    "gender": "male",
    "age": "middle",
    "languages": [
      "ar",
      "en"
    ],
    "dialect": [
      "emirati"
    ],
    "type": "neural",
    "sample_url": "https://pub-a2eeb500293c41df9e06b82b4178468a.r2.dev/samples/ar-saif-male.wav"
  },
  {
    "voice_id": "ar-uae-female-1",
    "name": "aisha",
    "description": "Emirati female voice with authentic Gulf dialect",
    "gender": "female",
    "age": "middle",
    "languages": [
      "ar",
      "en"
    ],
    "dialect": [
      "emirati"
    ],
    "type": "neural",
    "sample_url": "https://pub-a2eeb500293c41df9e06b82b4178468a.r2.dev/samples/ar-aisha-female.wav"
  }
]
```

## Usage

Use the `voice_id` from the response in text-to-speech generation endpoints:

- `POST /text-to-speech/:model_id` - Include `voice_id` in the request body
- `WS /text-to-speech` - Include `voice_id` in the WebSocket message

### JavaScript Example

```javascript
const response = await fetch('https://api.faseeh.ai/api/v1/voices', {
  method: 'GET',
  headers: {
    'x-api-key': 'YOUR_API_KEY',
  },
});

const voices = await response.json();
console.log('Available voices:', voices);

// Use a voice in text-to-speech request
const selectedVoice = voices.find(voice => voice.name === 'hassan');
```

### Python Example

```python
import requests

url = "https://api.faseeh.ai/api/v1/voices"
headers = {
    "x-api-key": "YOUR_API_KEY"
}

response = requests.get(url, headers=headers)

if response.status_code == 200:
    voices = response.json()
    print(f"Found {len(voices)} voices")
    
    # Filter voices by dialect
    emirati_voices = [v for v in voices if "emirati" in v.get("dialect", [])]
    print(f"Emirati voices: {[v['name'] for v in emirati_voices]}")
else:
    print(f"Error: {response.status_code} - {response.text}")
```

## Voice Types

Voices can be categorized by:

- **Dialect**: `fusha` (Modern Standard Arabic), `emirati`, `najdi`, `hijazi`, `kuwaiti`, `egyptian`, `british`, etc.
- **Gender**: `male` or `female`
- **Age**: `middle` or `elderly`
- **Languages**: Supported language codes (e.g., `ar` for Arabic, `en` for English)

<Note>
  **Custom Voices**: Some voices may have `null` values for certain fields. These are typically custom user-created voices. The `voice_id` can still be used in text-to-speech requests regardless of these field values.
</Note>

<Note>
  **Caching**: Voice information doesn't change frequently. Consider caching the voice list to reduce API calls and improve application performance.
</Note>

